{
  "name": "req-management",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lerna run dev --parallel"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@module-federation/enhanced": "^0.5.1"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
